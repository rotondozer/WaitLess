// -- IMPORTED BY ALL SIBLINGS `./*`
const baseUrl = __DEV__
  ? "http://10.0.2.2:4741" // 10.0.2.2 == localhost that makes AVD happy
  : "https://waitlist-api.herokuapp.com"; // TODO: verify this endpoint

export default baseUrl;
